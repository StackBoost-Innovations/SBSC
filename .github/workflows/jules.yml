name: Jules AI Integration

on:
  workflow_dispatch:      # This adds the "Invoke" button to your Actions tab
  issue_comment:          # This lets Jules respond to @jules comments in PRs
    types: [created]

jobs:
  jules_job:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
      issues: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Jules Invoke
        uses: google-labs-code/jules-action@v1.0.0
        with:
          jules_api_key: ${{ secrets.JULES_API_KEY }}

      - name: Jules PR Comment
        uses: google-labs-code/jules-publish@v1.0.0
        with:
          jules_api_key: ${{ secrets.JULES_API_KEY }}

      - name: Send Feedback to Jules
        uses: google-labs-code/jules-comms@v1.0.0
        with:
          jules_api_key: ${{ secrets.JULES_API_KEY }}
